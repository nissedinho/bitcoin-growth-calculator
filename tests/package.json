{
  "private": true,
  "name": "bitcoin-growth-calculator-tests",
  "description": "Test-only package. Deliberately NOT at the repo root: a root package.json turns the static site into a Node build on Vercel and breaks the deploy.",
  "type": "module",
  "scripts": {
    "test": "node run.mjs"
  },
  "devDependencies": {
    "jsdom": "^25.0.0"
  }
}
